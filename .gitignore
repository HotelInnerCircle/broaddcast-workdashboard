# --- Secrets: never commit real credentials. Only .env.example is tracked. ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# --- Dependencies & build output ---
node_modules
.next
out
dist
*.tsbuildinfo
next-env.d.ts

# --- Local runtime data ---
# dev email outbox + exports
.dev/
# legacy local storage (removed; kept ignored for old checkouts)
uploads/
*.log
.dev-server.log
.prod-server.log
npm-debug.log*
yarn-error.log*

# --- Test / coverage / tooling ---
coverage
.nyc_output
playwright-report
test-results
.eslintcache
.turbo
.vercel

# --- Editors & OS ---
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# --- AI assistant local state ---
.claude/settings.local.json

# --- Native (Capacitor): keep the projects, ignore build output and local SDK files ---
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
android/app/src/main/assets/public/
android/app/google-services.json
ios/App/build/
ios/App/Pods/
ios/App/App/public/
ios/App/App/GoogleService-Info.plist
*.keystore
*.jks
android/key.properties

# Desktop app (Electron) - build output and its own dependencies
desktop/node_modules/
desktop/dist/

# End-to-end runs: screenshots and the throwaway company details
.e2e
